[project]
name = "calculator"
version = "0.1.0"
description = "A simple calculator tool for learning GitHub Actions"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
